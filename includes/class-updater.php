<?php
/**
 * Updater stub.
 *
 * This class is intentionally empty. Updates are handled by wordpress.org.
 *
 * @package Theme_String_Translator
 * @since   1.0.0
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

/**
 * Class TST_Updater
 *
 * Stub class kept for compatibility. The GitHub updater has been removed
 * because wordpress.org handles plugin updates.
 *
 * @since 1.0.0
 */
class TST_Updater {

	/**
	 * Constructor.
	 *
	 * Intentionally empty — updates are managed by wordpress.org.
	 *
	 * @since 1.0.0
	 *
	 * @param string $plugin_file Absolute path to the main plugin file.
	 */
	public function __construct( $plugin_file ) {
		// No-op: updates are handled by wordpress.org.
	}
}
